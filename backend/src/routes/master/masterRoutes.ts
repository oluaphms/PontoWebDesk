/**
 * Rotas legadas Master — mantidas apenas como referência.
 * A montagem ativa usa backend/src/master/api (ver routes/master/index.ts).
 */
export { default } from '../../master/api/routes/masterApiRouter.js';
