/**
 * Porta do repositório de empresas Master.
 * Implementações: InMemory hoje; HTTP/Postgres no futuro.
 */
export type { TenantManagerStore as MasterTenantsStore } from '../../tenantManager/ports/TenantManagerStore.js';
