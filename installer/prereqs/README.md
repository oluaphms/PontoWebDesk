# Place aqui o instalador oficial da Docker Inc.:
#   DockerDesktopInstaller.exe
#
# Download (conta Docker / página oficial):
#   https://docs.docker.com/desktop/setup/install/windows-install/
#
# Sem este arquivo:
#   - O instalador PontoWebDesk ainda compila.
#   - Em Windows limpo, ensure-docker.ps1 NÃO consegue instalar Docker sozinho
#     e a instalação falha até o operador colocar o .exe ou instalar Docker manualmente.
#
# NÃO commitar o binário no git (arquivo grande / EULA Docker).
