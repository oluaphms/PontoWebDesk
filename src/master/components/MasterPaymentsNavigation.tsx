import React from 'react';

/**
 * Banner informativo de confirmação manual removido da UI.
 * Mantido o export para não quebrar imports existentes (MasterLayout).
 */
export function MasterPaymentsNavigation() {
  return null;
}
